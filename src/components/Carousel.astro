---
import { Image } from 'astro:assets';

const { images } = Astro.props; 
---

<div class="carousel">
    <div class="carousel-inner">
        {images.map((image) => (
            <div class="slide">
                <Image src={image} alt="image" layout="constrained" loading="lazy" width="1048px" height="911px" />
            </div>
        ))}
    </div>
    <div class="carousel-controls">
        <!-- <a href="/articles" class="return"><span class="arrow">←</span> Retour</a> -->
        <div class="dots">
            <div class="dotsInner">
                {images.map((image: any) => (
                    <span class={`dot dot-${(images.indexOf(image) + 1)}`}></span>
                ))}
            </div>
        </div>
        <div class="buttons">
            <button id="prev"><img src="/icon-arrow.svg"/></button>
            <button id="next"><img src="/icon-arrow.svg"/></button>
        </div>
    </div>
</div>

<script>

(function () {
    "use stict"
    const prev = document.querySelector('#prev');
    const next = document.querySelector('#next');
    const $slides = document.querySelectorAll('.slide');
    const allDots = document.querySelector('.dots');
    const $dots = allDots.querySelectorAll('.dot');
    let currentSlide = 0;
    $slides[currentSlide].classList.add("active");

    function slideTo(index) {
        currentSlide = index >= $slides.length ? ($slides.length - 1) : currentSlide = index < 0 ? 0 : index;
        // currentSlide = index >= $slides.length ? 0 : currentSlide = index < 0 ? ($slides.length - 1) : index;
        $slides.forEach($elt => $elt.style.transform = `translateX(-${currentSlide * 100}%)`);
        $dots.forEach($elt => $elt.style.transform = `translateX(-${currentSlide * 100}%)`);
    }

    prev.addEventListener('click', () => slideTo(--currentSlide))
    next.addEventListener('click', () => slideTo(++currentSlide))

    $slides.forEach($elt => {
        let startX;
        let endX;
        $elt.addEventListener('touchstart', (event) => {
            startX = event.touches[0].clientX;
        });
        $elt.addEventListener('touchend', (event) => {
            endX = event.changedTouches[0].clientX;
            if (startX > endX) {
                slideTo(currentSlide + 1);
            } else if (startX < endX) {
                slideTo(currentSlide - 1);
            }
        });
    })

})()
</script>

<style>
.carousel {
    position: relative;
}
.carousel-inner {
    display: flex;
    overflow: hidden;
    flex-wrap: nowrap;
    aspect-ratio: 1;
}
.slide {
    flex: 0 0 100%;
    height: 100%;
    transition: .5s ease-in-out;
}
.slide img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}
.carousel-controls {
    position: absolute;
    z-index: 5;
    left: 0;
    right: 0;
    top: 50%;
    display: flex;
    justify-content: space-between;
}
/* .carousel-controls button {
    padding: 13px;
    background-color: #fff;
    border: none;
    cursor: pointer;
    display: flex;
    justify-content: center;
    align-items: center;
} */
.carousel-controls button {
    padding: 13px;
    background-color: #000000;
    border: none;
    cursor: pointer;
    display: flex;
    justify-content: center;
    align-items: center;
}
/* .carousel-controls button img {
    width: 22px;
    height: 20px;
} */
.carousel-controls button img {
    width: 22px;
    height: 20px;
    filter: invert(1);
}
.carousel-controls button#next img {
    transform: rotate(180deg);
}

/* ------- DOTS ------- */ 
.dots {
    display: flex;
    width: 44px;
    flex-wrap: nowrap;
    justify-content: center;
    overflow: hidden;
    position: relative;
    mask-image: linear-gradient(to right, transparent 0%, black 50%, transparent 100%);
}
.dotsInner {
    width: 12px;
}
.dot {
    width: 12px;
    height: 6px;
    display: flex;
    flex: 0 0 100%;
    max-width: 12px;
    position: relative;
    transition: all 0.3s;
}
.dot:after {
    content: '';
    width: 6px;
    height: 6px;
    border-radius: 6px;
    background-color: #fff;
    position: absolute;
    left: 50%;
    top: 0;
    transform: translateX(-50%);
}

@media screen and (max-width: 768px) {
    .buttons {
        display: none;
    }
    .carousel-controls button {
        padding: 10px;
    }
}
</style>