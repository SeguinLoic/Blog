---
import { Image } from 'astro:assets';

const { images } = Astro.props; 
---

<div class="carousel">
    <div class="carousel-inner">
        {images.map((image) => (
            <div class="slide">
                <Image src={image} alt="image" layout="constrained" loading="lazy" width="1048px" height="911px" />
            </div>
        ))}
    </div>
    <div class="carousel-controls">
        <a href="/articles" class="return"><span class="arrow">←</span> Retour</a>
        <div>
            <button id="prev"><img src="/icon-arrow.svg"/></button>
            <button id="next"><img src="/icon-arrow.svg"/></button>
        </div>
    </div>
</div>

<script>

(function () {
    "use stict"
    const prev = document.querySelector('#prev');
    const next = document.querySelector('#next');
    const $slides = document.querySelectorAll('.slide');
    let currentSlide = 0;
    $slides[currentSlide].classList.add("active");

    function slideTo(index) {
        currentSlide = index >= $slides.length ? 0 : currentSlide = index < 0 ? ($slides.length - 1) : index;
        // $slides.forEach($elt => $elt.style.transform = `translateX(-${currentSlide * 100}%)`);
        $slides.forEach($elt => $elt.classList.remove("active"));
        $slides[currentSlide].classList.add("active");
    }

    function slideForth(index) {
        currentSlide = index >= $slides.length ? 0 : currentSlide = index < 0 ? ($slides.length - 1) : index;
        $slides.forEach($elt => $elt.classList.remove("active", "back", "forth"));
        $slides[currentSlide].classList.add("active", "forth");
    }

    function slideBack(index) {
        currentSlide = index >= $slides.length ? 0 : currentSlide = index < 0 ? ($slides.length - 1) : index;
        $slides.forEach($elt => $elt.classList.remove("active", "back", "forth"));
        $slides[currentSlide].classList.add("active", "back");
    }

    prev.addEventListener('click', () => slideBack(--currentSlide))
    next.addEventListener('click', () => slideForth(++currentSlide))

    // prev.addEventListener('click', () => slideTo(--currentSlide))
    // next.addEventListener('click', () => slideTo(++currentSlide))

    $slides.forEach($elt => {
        let startX;
        let endX;
        $elt.addEventListener('touchstart', (event) => {
            startX = event.touches[0].clientX;
        });
        $elt.addEventListener('touchend', (event) => {
            endX = event.changedTouches[0].clientX;
            if (startX > endX) {
                slideTo(currentSlide + 1);
            } else if (startX < endX) {
                slideTo(currentSlide - 1);
            }
        });
    })

})()
</script>

<style>
.carousel {
    position: relative;
}
.carousel-inner {
    display: flex;
    overflow: hidden;
    flex-wrap: nowrap;
    aspect-ratio: 1;
}
/* .slide {
    flex: 0 0 100%;
    height: 100%;
    transition: .5s ease-in-out;
} */
.slide {
    flex: 0 0 100%;
    height: 100%;
    position: absolute;
    transform: translateX(100%);
    pointer-events: none;
    animation: animSlideOut 0.4s linear;
}
@keyframes animSlideIn {
    0% {transform: translateX(100%); pointer-events: none;}
    100% {transform: translateX(0%); pointer-events: auto;}
}
@keyframes animSlideOut {
    0% {transform: translateX(0%); pointer-events: auto;}
    100% {transform: translateX(-100%); pointer-events: none;}
}
.slide.active {
    animation: animSlideIn 0.4s linear;
    animation-fill-mode: forwards;
}
.slide img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}
.carousel-controls {
    position: absolute;
    z-index: 5;
    left: 0;
    right: 0;
    top: 50%;
    display: flex;
    justify-content: space-between;
}
.carousel-controls button {
    padding: 13px;
    background-color: #fff;
    border: none;
    cursor: pointer;
    display: flex;
    justify-content: center;
    align-items: center;
}
.carousel-controls button img {
    width: 22px;
    height: 20px;
}
.carousel-controls button#next img {
    transform: rotate(180deg);
}
@media screen and (max-width: 768px) {
    .carousel-controls button {
        padding: 10px;
    }
}
</style>